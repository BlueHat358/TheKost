<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thekost">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pembayaran.MetodeTopUpActivity"></activity>
        <activity android:name=".Setting.TentangKamiActivity" />
        <activity android:name=".Setting.EditAkunActivity" />
        <activity android:name=".Pembayaran.DetailPembayaranActivity" />
        <activity android:name=".Pembayaran.MetodePembayaranActivity" />
        <activity android:name=".Setting.SettingActivity" />
        <activity android:name=".Content.ContentActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".LupaPassword.LupaPassActivity" />
        <activity android:name=".Daftar.DaftarActivity1" />
        <activity android:name=".Daftar.DaftarActivity" />
        <activity android:name=".Login.LoginActivity1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>